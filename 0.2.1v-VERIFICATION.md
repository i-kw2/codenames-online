# Codenames Bilingual 0.2.1v — Verification

## Scope
This release is a targeted UI/profile update over the supplied build. Core Codenames turn, reveal, scoring, win-condition, and Firebase room-action logic were not redesigned.

## Implemented
- Invite nickname instruction is explicitly black.
- Player Settings now has a blue **Save changes** button beside **Leave match**.
- Nickname/avatar edits are staged and committed only on Save, available in both `lobby` and `playing` room states.
- The profile commit uses one Realtime Database `update()` call for `name` + `avatarId`.
- Dark Mode uses the requested grayscale ramp: `#E4E4E4`, `#BFBFBF`, `#939393`, `#5B5B5B`, `#333332`, while Red/Blue identity colors remain unchanged.
- `assets/BG.png` is used as the shared background artwork for the requested page/game states and modal/overlay backdrops.
- App version/cache-buster updated to `0.2.1v` / `?v=0.2.1`.

## Verification performed
- `node --check` passed for every JavaScript file in `js/` and `tests/`.
- Existing zero-dependency game/UI test suite executed under a Node test harness: **41/41 passed**.
- Static HTML validation check: no duplicate IDs.
- Static release assertions confirm `settingsSavePlayerBtn`, `settingsSaveMessage`, `assets/BG.png`, the new dark palette, and `savePlayerProfile()` are present.
- CSS brace-balance check passed.
