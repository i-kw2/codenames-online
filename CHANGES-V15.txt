CODENAMES BILINGUAL — V15

Primary goal
- Restore the V13 visual color system while keeping the V14 lobby/features.

Visual fixes
- Restored V13 background/surface palette: #f5f0e7 / #fffdf8 / #efe8dc.
- Restored V13 team colors: Blue #28679b and Red #a8302f with their soft V13 variants.
- Replaced neon/lime V14 actions with the V13 dark olive primary (#303a2d).
- Settings, Rules, News and Quick Guide now use V13 cream/light surfaces instead of black panels.
- Preserved physical layout: Blue left, Red right, center settings, Spectators above.

Navigation / missing UI fixes
- Admin / News / Rules / Settings toolbar is now made visible directly by the realtime shell as well as the lobby UI layer.
- Settings remains accessible in the lobby and during an active online room.
- Added dialog fallback for browsers where showModal()/close() is unavailable or fails.
- Settings includes Admin / Player / Preferences / Accessibility and Reduced Motion.
- Rules content expanded to cover objective, setup, starting team, clues, n+1 guessing, wrong cards, winning/Assassin, clue legality and expert clues.

Realtime robustness fixes found during the V14 audit
- Only connected players count toward lobby validation and Start Game.
- Randomize Teams only distributes connected players; disconnected records are returned to Spectator.
- Old queued gameplay actions now carry an expiry time and are discarded if the Host reconnects after the client timeout.
- Returning to lobby unlocks teams and removes stale offline non-host records.

Safety / regression status
- V13 game.js is byte-for-byte unchanged in V15.
- V13 ui.js is byte-for-byte unchanged in V15.
- 38/38 game + UI logic tests pass.
- All JavaScript files pass syntax checks.
- firebase-rules.json parses successfully.
- No duplicate HTML IDs; all lobby-v15 cached IDs exist.
