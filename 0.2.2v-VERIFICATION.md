# 0.2.2v Verification

- JavaScript syntax: all project/test `.js` files pass `node --check`.
- Existing game/UI logic suite: **41/41 passed**.
- QR encoder: locally bundled QR matrix successfully decoded back to the sample `?room=ABC123` invite URL with OpenCV QRCodeDetector.
- QR flow: the encoded URL uses the same `?room=` format consumed by the existing Invite Nickname Gate (`#inviteEntryGate`).
- Player profile save: modern direct atomic save retained; permission-denied fallback now routes `saveProfile` through the room host, and successful lobby saves close Settings + focus `#lobbySection`.
- Join sound: fires only when a newly active remote player appears after the initial players snapshot, through the existing Effects volume/mute system.
- Background: `assets/BG.png` is byte-for-byte identical to the supplied image; `assets/BG-blur.png` has the same 1672×941 dimensions with a 10px Gaussian blur.
- HTML IDs: no duplicates; all new QR/profile elements exist.
- Cache/version: HTML meta is `0.2.2v`; CSS/JS cache-busters are `?v=0.2.2`.
