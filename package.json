{
  "name": "survey-showdown",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bilingual survey game for VS Code / browser, with optional Socket.IO online rooms.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "express": "^5.1.0",
    "socket.io": "^4.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
